# Практика 8. Транспортный уровень (сдать до 20.04.2023) 


## 3. Задачи 

### Задача 1 (2 балла) 
**
Пусть T (измеряется в RTT) обозначает интервал времени, который TCP-соединение тратит на
увеличение размера окна перегрузки с W/2 до W, где W – это максимальный размер окна
перегрузки. Докажите, что T – это функция от средней пропускной способности TCP. **


Из лекции мы знаем, что $V_{mean} = $ средняя пропускная способность канала равна = $\frac{3}{4} \frac{W}{RTT} \frac{\text{байт}}{\text{с}}$

По условию $T$ - измеряется в $RTT$ $\Rightarrow$ $T = const * RTT$ $\Rightarrow$ $RTT = \frac{3}{4} \frac{W}{V_{mean}}$ $\Rightarrow$ $T = \frac{3}{4} \frac{const * W}{V_{mean}}$


### Задача 3 (2 балла)

**Рассмотрим модификацию алгоритма управления перегрузкой протокола TCP. Вместо
аддитивного увеличения, мы можем использовать мультипликативное увеличение. TCPотправитель увеличивает размер своего окна в (1+ a) раз (где a - небольшая положительная
константа (0<a<1)), как только получает верный ACK-пакет.
Найдите функциональную зависимость между частотой потерь L и максимальным размером окна
перегрузки W. Утверждается, что для этого измененного протокола TCP, независимо от средней
пропускной способности TCP-соединения всегда требуется одинаковое количество времени для
увеличения размера окна перегрузки с W/2 до W. **

Изначальный размер окна $\frac{W}{2}$, конечный $W$, предпологаем, что размер окна увеличивается в $(1 + a), 0 < a < 1$. Тогда 
при увелечении окна $k$ - раз, размер окна будет равен $\frac{W}{2} (1 + a) ^ k$. Возьмём такое $k + 1$, чтобы впервые наше окно превысило размер максимального. 

$\frac{W}{2} (1 + a) ^ k \leq W < \frac{W}{2} (1 + a) ^ {k + 1}$. Тогда  $k * RTT$ - это величина задержки между двумя соседними потерями [При условие, что RTT const]. А частота, соответсвенно обратная к ней $L = \frac{1}{k * RTT}$ 

Соответсвенно $(1 + a)^k \leq 2 \Rightarrow k = \lfloor \frac{\log_2(2)}{\log_2(1 + a)} \rfloor = \lfloor \frac{1}{\log_2(1 + a)} \rfloor$

$$L = \frac{1}{\lfloor \frac{1}{\log_2(1 + a)} \rfloor * RTT}$$


Задача 4. Расслоение TCP (2 балла)

В целом, всё объяснение лежит в объяснении задачи, соберём всё вместе. 

* Установление соединениемежду клиентом и внешним сервером нужно $4 * RTT\_ FE $
    * RTT_FE - открытие TCP соединения
    * 3 * RTT_FE - [обычно 3 окна нужно на этапе медленого старта]
* RTT_BE для обмена данными между внешним сервисом и дата-центром [между ними есть постоянное TCP-соединение]

* Время обработки запроса самим дата-центра [от этого уж никуда не убежишь] 

В итоге так и получается.